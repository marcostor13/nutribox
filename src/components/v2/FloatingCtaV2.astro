---
import { Icon } from 'astro-icon/components';
import { waLink } from '../../utils/whatsapp';
---

<a
  id="bunker-floating-cta"
  href={waLink('Hola, quiero agendar mi evaluación gratuita en BUNKERGYM.')}
  target="_blank"
  rel="noopener noreferrer"
  class="fixed bottom-6 right-6 z-30 flex translate-y-24 -rotate-3 items-center gap-2 border-[3px] border-void bg-acid px-5 py-3.5 font-display text-sm font-black uppercase tracking-tight text-acid-ink opacity-0 shadow-[6px_6px_0_0_var(--color-void)] transition-[transform,opacity] duration-500 hover:rotate-0 sm:right-8 sm:bottom-8"
  aria-label="Agenda tu evaluación gratuita por WhatsApp"
>
  <Icon name="lucide:message-circle" class="h-4 w-4" />
  <span class="hidden sm:inline">Agenda tu evaluación</span>
  <span class="sm:hidden">Agenda ahora</span>
</a>

<script>
  const cta = document.getElementById('bunker-floating-cta');
  const hero = document.querySelector('[data-hero]');

  if (cta && hero) {
    const observer = new IntersectionObserver(
      ([entry]) => {
        if (entry.isIntersecting) {
          cta.classList.add('translate-y-24', 'opacity-0');
        } else {
          cta.classList.remove('translate-y-24', 'opacity-0');
        }
      },
      { threshold: 0 }
    );
    observer.observe(hero);
  } else if (cta) {
    cta.classList.remove('translate-y-24', 'opacity-0');
  }
</script>
